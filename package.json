{
  "name": "chieftan",
  "version": "0.1.0",
  "description": "Your open-source AI Chief of Staff — private, safe, extensible personal agent built on Claude Agent SDK",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "chieftan": "./node_modules/.bin/tsx src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "build:ui": "cd ui && npm run build",
    "dev": "npm run build:ui && tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/static": "^9.0.0",
    "better-sqlite3": "^11.8.1",
    "croner": "^9.0.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "grammy": "^1.35.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.13.4",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
